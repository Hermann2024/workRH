server:
  port: 9087
spring:
  datasource:
    url: jdbc:postgresql://localhost:6438/workrh_subscriptions
    username: workrh
    password: workrh
subscription:
  user-service:
    base-url: ${USER_SERVICE_BASE_URL:http://localhost:9081}
  workspace:
    internal-key: ${WORKRH_WORKSPACE_INTERNAL_KEY}
  preview-all-features:
    enabled: ${WORKRH_PREVIEW_ALL_FEATURES_ENABLED:false}
    user-emails: ${WORKRH_PREVIEW_ALL_FEATURES_USER_EMAILS:}
  bootstrap:
    key: ${WORKRH_SUBSCRIPTION_BOOTSTRAP_KEY}
