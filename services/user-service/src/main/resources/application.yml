spring:
  application:
    name: user-service
  config:
    import: optional:configserver:http://localhost:9888

subscription:
  service:
    base-url: ${SUBSCRIPTION_SERVICE_BASE_URL:http://localhost:9087}
  bootstrap:
    key: ${WORKRH_SUBSCRIPTION_BOOTSTRAP_KEY:workrh-signup-bootstrap}
